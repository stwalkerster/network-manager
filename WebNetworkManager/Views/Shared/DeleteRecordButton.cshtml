@model DnsWebApp.Models.Dns.RecordViewModelBase

<div class="btn-toolbar mb-2 mb-md-0">
    <a href="@Url.Action("DeleteRecord", "Record", new {item = Model.Id})" class="btn btn-sm btn-danger ml-2">
        <i class="fas fa-trash"></i>&nbsp;Delete record
    </a>
</div>
@model DnsWebApp.Models.ViewModels.ZoneGroupDisplay
@{
    ViewBag.Title = "Zone Group";
    Layout = "_Layout";
    ViewBag.Controller = "ZoneGroup";
    ViewBag.Id = Model.ZoneGroup.Id;
}

<div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
    <h1 class="h2">Zone Group @(Model.ZoneGroup.Name)</h1>
</div>

<partial name="ShowZoneRecords" />